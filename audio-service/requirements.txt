# Audio analysis micro-service. Python 3.10 (essentia-tensorflow ships Linux
# wheels only up to cp310; it statically bundles TensorFlow, so it does NOT pull a
# conflicting tensorflow/protobuf — its only deps are numpy/six/pyyaml).
fastapi>=0.110
uvicorn[standard]>=0.29
librosa>=0.10
numpy>=1.24,<2     # <2 for the essentia bundled-TF + numba ABI
soundfile>=0.12
matplotlib>=3.7
faster-whisper>=1.0
httpx>=0.27        # calls the Music Flamingo HF Space (raw gradio queue API)
demucs>=4.0        # stem separation (Stem Lab); pulls torch/torchaudio
python-multipart>=0.0.9  # FastAPI file uploads (/upload endpoint)
essentia-tensorflow==2.1b6.dev858  # Discogs-EffNet + MTG-Jamendo discriminative taggers
transformers>=4.40,<5   # LAION music-CLAP (text+audio embeddings) for sonic search/twins
pyrubberband>=0.3   # Mashup Lab: HQ time-stretch/pitch-shift (wraps the rubberband-cli binary)
# torch + torchaudio come with demucs; on Apple Silicon they use the MPS GPU
